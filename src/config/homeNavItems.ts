import { MainNavConfig } from '@/types';

export const mainNavConfig: MainNavConfig = {
  mainNav: [
    {
      title: 'Home',
      to: '/',
    },
    {
      title: 'Tickets',
      to: '/tickets',
    },
  ],
};
